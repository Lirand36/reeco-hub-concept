{
  "name": "frontline-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Frontline Hub: the internal Sales, Support & CS workspace on top of HubSpot, Intercom, Jira, Slack, Snowflake and Claude.",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  }
}
